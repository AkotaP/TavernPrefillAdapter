{
  "name": "tavern-prefill-adapter",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "SillyTavern extension: unified assistant/reasoning prefill translation per provider.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
